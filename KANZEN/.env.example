# --- Configuration Serveur ---
NODE_ENV=production
PORT=3000

# --- Base de Données (Prisma + SQLite) ---
# En local : "file:./data/KanZen.db"
# Dans Docker : "file:/app/data/KanZen.db"
DATABASE_URL="file:./data/KanZen.db"

# --- Sécurité (JWT) ---
# Générez une clé forte (ex: openssl rand -base64 32)
JWT_SECRET="change_this_to_a_secure_random_string"